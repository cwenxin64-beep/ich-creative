# lessons

- 用户明确纠正“fyfy / XQ 迁移来的这些工作流都是玩非遗，不是创非遗”后，不能再把相似的 fyfy 工作流硬映射到创非遗；创非遗没有专用工作流时应直接走产品本体生图。
- 用户确认 fyfy 系列也属于玩非遗时，应接到玩非遗入口；更稳的方式是作为同类补充工作流加入，不覆盖已接好的 XQ 系列。
- 用户指出创非遗的“体验类型/传承类型”逻辑不顺时，应改成“风格”而不是只改文案；前后端生成提示都要按风格理解。
- 需求单图片不能只依赖用户手动选择参考作品；有当前生成结果时应默认带第一张图，并在展示端兼容 mainImageUrl/imageUrl/url 等字段。
- 拍非遗上传实物图时，核心需求是“保留原物体后叠加纹样”，不能让模型重新设计同类产品；提示词必须显式禁止把普通杯子变成保温杯、礼盒、包装等。
- 页面自动加载失败但不影响继续使用时，不要弹一闪而过的错误提示；保留手动操作失败提示即可。
- 小程序音频生成页应直接播放后端返回的可播放地址，不能再次强行套代理；音频代理必须支持 Range 分段读取。
- 小程序长耗时生成任务不能只放在页面内存里；要保存 taskId，页面切回时继续查状态。
- 玩非遗同一产品类型不要默认返回多个工作流结果；用户确认更准确的结果后，只保留对应工作流。
- 拍非遗最终生图模型如果只收到文字 Prompt，就必须把识图得到的原图主体和视觉锚点显式写进 Prompt；不能只写“保留上传图片”，否则模型会按常见模板自由发挥。
